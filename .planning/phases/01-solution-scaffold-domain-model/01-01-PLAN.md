---
phase: 01-solution-scaffold-domain-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/StorageService.sln
  - backend/Directory.Build.props
  - backend/src/Storage.Domain/Storage.Domain.csproj
  - backend/src/Storage.Application/Storage.Application.csproj
  - backend/src/Storage.Application/Abstractions/.gitkeep
  - backend/src/Storage.Infrastructure.Persistence.SqlServer/Storage.Infrastructure.Persistence.SqlServer.csproj
  - backend/src/Storage.Infrastructure.Storage.Wasabi/Storage.Infrastructure.Storage.Wasabi.csproj
  - backend/src/Storage.Infrastructure.Storage.AzureBlob/Storage.Infrastructure.Storage.AzureBlob.csproj
  - backend/src/Storage.Infrastructure.Storage.FileSystem/Storage.Infrastructure.Storage.FileSystem.csproj
  - backend/src/Storage.Infrastructure.Cache.Redis/Storage.Infrastructure.Cache.Redis.csproj
  - backend/src/Storage.Infrastructure.Cache.InMemory/Storage.Infrastructure.Cache.InMemory.csproj
  - backend/src/Storage.Infrastructure.Messaging.RabbitMQ/Storage.Infrastructure.Messaging.RabbitMQ.csproj
  - backend/src/Storage.Infrastructure.Messaging.AzureServiceBus/Storage.Infrastructure.Messaging.AzureServiceBus.csproj
  - backend/src/Storage.Sdk/Storage.Sdk.csproj
  - backend/src/Storage.Api/Storage.Api.csproj
  - backend/src/Storage.Gateway/Storage.Gateway.csproj
  - backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj
  - backend/tests/Storage.Domain.Tests/FileStatusTransitionTests.cs
  - backend/tests/Storage.Domain.Tests/StorageKeyTests.cs
  - backend/tests/Storage.Domain.Tests/ChecksumTests.cs
  - backend/tests/Storage.Domain.Tests/FileCategoryValidationTests.cs
  - backend/tests/Storage.Domain.Tests/DomainEventTests.cs
  - frontend/angular.json
  - frontend/package.json
  - frontend/projects/demo-app/
  - frontend/projects/shared-lib/
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02

must_haves:
  truths:
    - "`dotnet build backend/StorageService.sln -warnaserror` exits 0 with zero warnings"
    - "All 12 src projects + Storage.Gateway + Storage.Domain.Tests are members of StorageService.sln"
    - "`Storage.Domain.csproj` and `Storage.Application.csproj` contain zero `<ProjectReference>` to any `Storage.Infrastructure.*` project — enforced by Directory.Build.targets"
    - "`Storage.Application/Abstractions/` directory exists with `.gitkeep` — no port interfaces yet"
    - "Angular workspace scaffolded at `frontend/` with `demo-app` app and `shared-lib` library"
    - "Test stub files exist and compile; `dotnet test Storage.Domain.Tests` runs 0 tests (all are TODO stubs)"
  artifacts:
    - path: "backend/StorageService.sln"
      provides: ".sln format solution file (not .slnx)"
    - path: "backend/Directory.Build.props"
      provides: "MSBuild property sheet setting common TargetFramework and enforcing domain/app isolation"
    - path: "backend/src/Storage.Domain/Storage.Domain.csproj"
      provides: "net10.0 classlib; zero ProjectReferences"
    - path: "backend/src/Storage.Application/Storage.Application.csproj"
      provides: "net10.0 classlib; references only Storage.Domain"
    - path: "backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj"
      provides: "xUnit v3 test project; references Storage.Domain only"
    - path: "frontend/angular.json"
      provides: "Angular 22 workspace with shared-lib library and demo-app app"
  key_links:
    - from: "backend/Directory.Build.props"
      to: "Storage.Domain.csproj, Storage.Application.csproj"
      via: "MSBuild targets that error on Infrastructure references"
      pattern: "Storage\\.Infrastructure"
    - from: "Storage.Application.csproj"
      to: "Storage.Domain.csproj"
      via: "<ProjectReference>"
      pattern: "ProjectReference.*Storage\\.Domain"
---

<objective>
Scaffold the complete backend .NET 10 solution (13 projects), the Angular 22 frontend workspace, and the Wave 0 xUnit test project with stub test files. No production logic is written in this plan — only project files, references, and test scaffolding.

Purpose: Establish the correct dependency graph before any code is written. The `Directory.Build.props`/targets isolation guard makes it impossible to accidentally add infrastructure references to the domain or application projects.

Output: `backend/StorageService.sln` with 14 project entries (13 src + 1 test); Angular workspace at `frontend/`; `Storage.Domain.Tests` with stub files that compile but run 0 tests.
</objective>

<execution_context>
@C:/Users/ahmed.hhelmy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ahmed.hhelmy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-solution-scaffold-domain-model/01-CONTEXT.md
@.planning/phases/01-solution-scaffold-domain-model/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold .NET 10 solution with all 13 src projects + project references</name>
  <files>
    backend/StorageService.sln,
    backend/Directory.Build.props,
    backend/src/Storage.Domain/Storage.Domain.csproj,
    backend/src/Storage.Application/Storage.Application.csproj,
    backend/src/Storage.Application/Abstractions/.gitkeep,
    backend/src/Storage.Infrastructure.Persistence.SqlServer/Storage.Infrastructure.Persistence.SqlServer.csproj,
    backend/src/Storage.Infrastructure.Storage.Wasabi/Storage.Infrastructure.Storage.Wasabi.csproj,
    backend/src/Storage.Infrastructure.Storage.AzureBlob/Storage.Infrastructure.Storage.AzureBlob.csproj,
    backend/src/Storage.Infrastructure.Storage.FileSystem/Storage.Infrastructure.Storage.FileSystem.csproj,
    backend/src/Storage.Infrastructure.Cache.Redis/Storage.Infrastructure.Cache.Redis.csproj,
    backend/src/Storage.Infrastructure.Cache.InMemory/Storage.Infrastructure.Cache.InMemory.csproj,
    backend/src/Storage.Infrastructure.Messaging.RabbitMQ/Storage.Infrastructure.Messaging.RabbitMQ.csproj,
    backend/src/Storage.Infrastructure.Messaging.AzureServiceBus/Storage.Infrastructure.Messaging.AzureServiceBus.csproj,
    backend/src/Storage.Sdk/Storage.Sdk.csproj,
    backend/src/Storage.Api/Storage.Api.csproj,
    backend/src/Storage.Gateway/Storage.Gateway.csproj
  </files>
  <action>
    Run the following commands from the repo root (PowerShell) in order:

    1. Create the solution (MUST use `--format sln` — .NET 10 defaults to .slnx which some tooling doesn't support):
       ```
       dotnet new sln -n StorageService -o backend --format sln
       ```

    2. Create `backend/Directory.Build.props` with these contents:
       ```xml
       <Project>
         <PropertyGroup>
           <TargetFramework>net10.0</TargetFramework>
           <Nullable>enable</Nullable>
           <ImplicitUsings>enable</ImplicitUsings>
           <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
         </PropertyGroup>
       </Project>
       ```

    3. Create `backend/Directory.Build.targets` with a build error guard that prevents domain/application from referencing infrastructure:
       ```xml
       <Project>
         <Target Name="CheckInfrastructureReferences" BeforeTargets="Build"
                 Condition="'$(MSBuildProjectName)' == 'Storage.Domain' Or '$(MSBuildProjectName)' == 'Storage.Application'">
           <ItemGroup>
             <InfraRefs Include="@(ProjectReference)" Condition="$([System.String]::Copy('%(Identity)').Contains('Storage.Infrastructure'))" />
           </ItemGroup>
           <Error Condition="@(InfraRefs->Count()) &gt; 0"
                  Text="$(MSBuildProjectName) must not reference any Storage.Infrastructure.* project. Found: @(InfraRefs)" />
         </Target>
       </Project>
       ```

    4. Create 10 classlib projects (run each individually):
       ```
       dotnet new classlib -n Storage.Domain -f net10.0 -o backend/src/Storage.Domain
       dotnet new classlib -n Storage.Application -f net10.0 -o backend/src/Storage.Application
       dotnet new classlib -n Storage.Infrastructure.Persistence.SqlServer -f net10.0 -o backend/src/Storage.Infrastructure.Persistence.SqlServer
       dotnet new classlib -n Storage.Infrastructure.Storage.Wasabi -f net10.0 -o backend/src/Storage.Infrastructure.Storage.Wasabi
       dotnet new classlib -n Storage.Infrastructure.Storage.AzureBlob -f net10.0 -o backend/src/Storage.Infrastructure.Storage.AzureBlob
       dotnet new classlib -n Storage.Infrastructure.Storage.FileSystem -f net10.0 -o backend/src/Storage.Infrastructure.Storage.FileSystem
       dotnet new classlib -n Storage.Infrastructure.Cache.Redis -f net10.0 -o backend/src/Storage.Infrastructure.Cache.Redis
       dotnet new classlib -n Storage.Infrastructure.Cache.InMemory -f net10.0 -o backend/src/Storage.Infrastructure.Cache.InMemory
       dotnet new classlib -n Storage.Infrastructure.Messaging.RabbitMQ -f net10.0 -o backend/src/Storage.Infrastructure.Messaging.RabbitMQ
       dotnet new classlib -n Storage.Infrastructure.Messaging.AzureServiceBus -f net10.0 -o backend/src/Storage.Infrastructure.Messaging.AzureServiceBus
       dotnet new classlib -n Storage.Sdk -f net10.0 -o backend/src/Storage.Sdk
       ```

    5. Create the API host (minimal API is the default in .NET 10 — do NOT pass `--use-minimal-apis`, that flag was removed in .NET 7+):
       ```
       dotnet new webapi -n Storage.Api -f net10.0 -o backend/src/Storage.Api
       ```

    6. Create the Ocelot gateway host (separate ASP.NET Core minimal API project — NOT hosted inside Storage.Api; Pitfall 3 from RESEARCH):
       ```
       dotnet new web -n Storage.Gateway -f net10.0 -o backend/src/Storage.Gateway
       ```
       NOTE: `Storage.Gateway` is not in the original 12-project CONTEXT list, but RESEARCH §Pattern 6 and Pitfall 3 explicitly require Ocelot in its own host project. Add it.

    7. Wire project references (only what is correct now — all Infrastructure.* projects reference Storage.Application; Storage.Application references Storage.Domain; Storage.Sdk references Storage.Application; Storage.Api references all Infrastructure.* and Storage.Application):
       ```
       dotnet add backend/src/Storage.Application/Storage.Application.csproj reference backend/src/Storage.Domain/Storage.Domain.csproj
       dotnet add backend/src/Storage.Infrastructure.Persistence.SqlServer/Storage.Infrastructure.Persistence.SqlServer.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Storage.Wasabi/Storage.Infrastructure.Storage.Wasabi.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Storage.AzureBlob/Storage.Infrastructure.Storage.AzureBlob.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Storage.FileSystem/Storage.Infrastructure.Storage.FileSystem.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Cache.Redis/Storage.Infrastructure.Cache.Redis.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Cache.InMemory/Storage.Infrastructure.Cache.InMemory.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Messaging.RabbitMQ/Storage.Infrastructure.Messaging.RabbitMQ.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Infrastructure.Messaging.AzureServiceBus/Storage.Infrastructure.Messaging.AzureServiceBus.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Sdk/Storage.Sdk.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Application/Storage.Application.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Persistence.SqlServer/Storage.Infrastructure.Persistence.SqlServer.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Storage.Wasabi/Storage.Infrastructure.Storage.Wasabi.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Storage.AzureBlob/Storage.Infrastructure.Storage.AzureBlob.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Storage.FileSystem/Storage.Infrastructure.Storage.FileSystem.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Cache.Redis/Storage.Infrastructure.Cache.Redis.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Cache.InMemory/Storage.Infrastructure.Cache.InMemory.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Messaging.RabbitMQ/Storage.Infrastructure.Messaging.RabbitMQ.csproj
       dotnet add backend/src/Storage.Api/Storage.Api.csproj reference backend/src/Storage.Infrastructure.Messaging.AzureServiceBus/Storage.Infrastructure.Messaging.AzureServiceBus.csproj
       ```

    8. Add all projects to the solution (PowerShell: list each path individually since ** glob is not supported):
       ```
       dotnet sln backend/StorageService.sln add backend/src/Storage.Domain/Storage.Domain.csproj backend/src/Storage.Application/Storage.Application.csproj backend/src/Storage.Infrastructure.Persistence.SqlServer/Storage.Infrastructure.Persistence.SqlServer.csproj backend/src/Storage.Infrastructure.Storage.Wasabi/Storage.Infrastructure.Storage.Wasabi.csproj backend/src/Storage.Infrastructure.Storage.AzureBlob/Storage.Infrastructure.Storage.AzureBlob.csproj backend/src/Storage.Infrastructure.Storage.FileSystem/Storage.Infrastructure.Storage.FileSystem.csproj backend/src/Storage.Infrastructure.Cache.Redis/Storage.Infrastructure.Cache.Redis.csproj backend/src/Storage.Infrastructure.Cache.InMemory/Storage.Infrastructure.Cache.InMemory.csproj backend/src/Storage.Infrastructure.Messaging.RabbitMQ/Storage.Infrastructure.Messaging.RabbitMQ.csproj backend/src/Storage.Infrastructure.Messaging.AzureServiceBus/Storage.Infrastructure.Messaging.AzureServiceBus.csproj backend/src/Storage.Sdk/Storage.Sdk.csproj backend/src/Storage.Api/Storage.Api.csproj backend/src/Storage.Gateway/Storage.Gateway.csproj
       ```

    9. Create the Abstractions folder stub (Phase 2 owns the actual interfaces — do NOT create any .cs interface files here):
       Create file `backend/src/Storage.Application/Abstractions/.gitkeep` (empty file).

    10. Delete the auto-generated `Class1.cs` placeholder from each classlib project (dotnet new classlib creates one). Also remove the WeatherForecast.cs from Storage.Api if present.

    11. Run `dotnet build backend/StorageService.sln -warnaserror` — must exit 0.
  </action>
  <verify>
    <automated>dotnet build backend/StorageService.sln -warnaserror</automated>
  </verify>
  <done>
    - `backend/StorageService.sln` exists in .sln format (not .slnx)
    - All 13 source projects exist under `backend/src/`; `Storage.Gateway` exists as the 13th
    - `backend/Directory.Build.targets` exists with the infrastructure reference guard
    - `Storage.Domain.csproj` and `Storage.Application.csproj` contain zero `&lt;ProjectReference&gt;` to `Storage.Infrastructure.*`
    - `backend/src/Storage.Application/Abstractions/.gitkeep` exists; no interface .cs files in that folder
    - `dotnet build backend/StorageService.sln -warnaserror` passes with 0 errors and 0 warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Angular 22 workspace with shared-lib library and demo-app</name>
  <files>
    frontend/angular.json,
    frontend/package.json,
    frontend/projects/demo-app/src/app/app.component.ts,
    frontend/projects/shared-lib/src/public-api.ts
  </files>
  <action>
    From the repo root, run:

    1. Create the Angular workspace with a demo-app application (standalone components, SCSS, routing):
       ```
       ng new demo-app --directory frontend --standalone --routing --style scss --skip-git
       ```

    2. Inside the frontend directory, generate the shared library:
       ```
       cd frontend
       ng generate library shared-lib
       ```

    3. Verify the workspace structure:
       - `frontend/angular.json` must have both `demo-app` (type: application) and `shared-lib` (type: library) projects
       - `frontend/projects/shared-lib/src/public-api.ts` must exist
       - `frontend/projects/demo-app/src/app/app.component.ts` must exist

    Note: Angular runs only via `ng serve` — it is NOT added to docker-compose.yml (per locked decision in CONTEXT.md).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; ng build shared-lib --configuration production 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `frontend/angular.json` lists both `demo-app` (application) and `shared-lib` (library)
    - `ng build shared-lib` succeeds
    - `frontend/projects/shared-lib/src/public-api.ts` exists as the library's public barrel export
  </done>
</task>

<task type="auto">
  <name>Task 3: Create xUnit v3 test project with Wave 0 stub test files</name>
  <files>
    backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj,
    backend/tests/Storage.Domain.Tests/FileStatusTransitionTests.cs,
    backend/tests/Storage.Domain.Tests/StorageKeyTests.cs,
    backend/tests/Storage.Domain.Tests/ChecksumTests.cs,
    backend/tests/Storage.Domain.Tests/FileCategoryValidationTests.cs,
    backend/tests/Storage.Domain.Tests/DomainEventTests.cs
  </files>
  <action>
    1. Create the xUnit v3 test project:
       ```
       dotnet new xunit -n Storage.Domain.Tests -f net10.0 -o backend/tests/Storage.Domain.Tests
       ```

    2. Replace the default `xunit` package reference in the generated `.csproj` with `xunit.v3` 3.2.x:
       Open `backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj` and replace the PackageReference for `xunit` with:
       ```xml
       <PackageReference Include="xunit.v3" Version="3.2.*" />
       <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5">
         <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
         <PrivateAssets>all</PrivateAssets>
       </PackageReference>
       <PackageReference Include="FluentAssertions" Version="8.9.*" />
       ```

    3. Add project reference to Storage.Domain:
       ```
       dotnet add backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj reference backend/src/Storage.Domain/Storage.Domain.csproj
       ```

    4. Add the test project to the solution:
       ```
       dotnet sln backend/StorageService.sln add backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj
       ```

    5. Create stub test files. Each file must compile (empty test class with correct namespace). Do NOT write actual test logic — that belongs to Plan 02 (TDD). These stubs are Wave 0 scaffolding only.

       `backend/tests/Storage.Domain.Tests/FileStatusTransitionTests.cs`:
       ```csharp
       using Xunit;

       namespace Storage.Domain.Tests;

       public class FileStatusTransitionTests
       {
           // Tests implemented in Plan 02 (TDD wave)
       }
       ```

       `backend/tests/Storage.Domain.Tests/StorageKeyTests.cs`:
       ```csharp
       using Xunit;

       namespace Storage.Domain.Tests;

       public class StorageKeyTests
       {
           // Tests implemented in Plan 02 (TDD wave)
       }
       ```

       `backend/tests/Storage.Domain.Tests/ChecksumTests.cs`:
       ```csharp
       using Xunit;

       namespace Storage.Domain.Tests;

       public class ChecksumTests
       {
           // Tests implemented in Plan 02 (TDD wave)
       }
       ```

       `backend/tests/Storage.Domain.Tests/FileCategoryValidationTests.cs`:
       ```csharp
       using Xunit;

       namespace Storage.Domain.Tests;

       public class FileCategoryValidationTests
       {
           // Tests implemented in Plan 02 (TDD wave)
       }
       ```

       `backend/tests/Storage.Domain.Tests/DomainEventTests.cs`:
       ```csharp
       using Xunit;

       namespace Storage.Domain.Tests;

       public class DomainEventTests
       {
           // Tests implemented in Plan 02 (TDD wave)
       }
       ```

    6. Run `dotnet build backend/StorageService.sln -warnaserror` — must still pass. The test project compiles but has no executable tests yet.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj --no-build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `Storage.Domain.Tests.csproj` uses xUnit v3 (`xunit.v3 3.2.*`) and FluentAssertions 8.9.*
    - All five stub test files exist and compile
    - `dotnet test` reports 0 tests executed (all stub classes have no [Fact] methods yet)
    - `dotnet build backend/StorageService.sln -warnaserror` still passes after adding test project
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `dotnet build backend/StorageService.sln -warnaserror` — exits 0, zero warnings, zero errors
2. `dotnet test backend/tests/Storage.Domain.Tests/Storage.Domain.Tests.csproj` — exits 0 (0 tests run)
3. Confirm `Storage.Domain.csproj` and `Storage.Application.csproj` have no `&lt;ProjectReference&gt;` to `Storage.Infrastructure.*` — inspect with: `Select-String "Storage.Infrastructure" backend/src/Storage.Domain/Storage.Domain.csproj backend/src/Storage.Application/Storage.Application.csproj`
4. Confirm `backend/src/Storage.Application/Abstractions/.gitkeep` exists and the Abstractions folder has no .cs files
5. Confirm `frontend/angular.json` exists with both `demo-app` and `shared-lib` entries
</verification>

<success_criteria>
- All 13 src projects + `Storage.Gateway` + `Storage.Domain.Tests` are members of `backend/StorageService.sln`
- `dotnet build` passes with zero warnings (TreatWarningsAsErrors=true enforced)
- Domain/Application isolation enforced by `Directory.Build.targets` — any attempt to add an Infrastructure reference to Storage.Domain or Storage.Application will fail the build
- Angular workspace exists at `frontend/` with `shared-lib` library
- Wave 0 test stubs compile and run 0 tests (ready for Plan 02 TDD work)
</success_criteria>

<output>
After completion, create `.planning/phases/01-solution-scaffold-domain-model/01-01-SUMMARY.md`
</output>
